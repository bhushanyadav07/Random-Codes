$currentMonth = date('m');
$currentYear = date('Y');

$currentMonthDates = array_filter($dates, function($date) use ($currentMonth, $currentYear) {
    $dateParts = explode('-', $date);
    return $dateParts[1] == $currentMonth && $dateParts[0] == $currentYear;
});
